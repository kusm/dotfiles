.:
  <src>:
    .bash_profile: bash_profile
    .bashrc: bashrc
    .bash.d: bash
    .wgetrc: wgetrc
    .brewfile: brewfile
.ssh:
  ssh_public_keys:
    authorized_keys: authorized_keys
Library:
  <src>:
    bin: bin
